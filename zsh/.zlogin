# sourced on login shells (it is called after `.zshrc`)
# do not configure environment or shell
# use to set terminal type and run external commands (i.e. record who logged in, activate some script etc.)
# use as 'login' hook

echo "Welcome, $USER!"

